# --- RaW Suite Master Requirements ---
setuptools

# Núcleo y TUI
rich
python-dotenv
requests[socks]>=2.28.0
PySocks

# Web Dashboard
fastapi
uvicorn
jinja2
beautifulsoup4

# RawLoadrr & Subida
aiofiles
aiohttp
anitopy
bencode.py
# PINNED: cinemagoer 2026.06.27 removed the HTTP access system entirely and switched the
# default accessSystem from 'http' to 's3'. Cinemagoer() then builds an SQLAlchemy engine
# from the bogus default URI 'sqlite://cinemagoer.db', which SQLAlchemy 2.x rejects, so
# every construction raises ArgumentError before any lookup happens. Do not unpin.
cinemagoer==2023.5.1
deluge-client
ffmpeg-python
guessit
language-data<=1.2.0; python_version<='3.8'
langcodes<=3.4.0; python_version<='3.8'
langcodes; python_version>'3.8'
nest_asyncio
packaging
pyimgbox
pyotp
pyoxipng
pymediainfo
qbittorrent-api<=2023.9.53; python_version<'3.8'
qbittorrent-api; python_version>='3.8'
tinytag
torf
tmdbsimple
unidecode

# Recordrr — automatización de captura por navegador
# (usa Google Chrome del sistema vía channel="chrome"; NO descargar Chromium)
playwright
opencv-python-headless
simpleobsws

# Procesamiento de Video (VapourSynth)
# Nota: En Docker instalaremos la base por apt-get, 
# pero estas librerías de python son los puentes.
vapoursynth
awsmfunc
