# --- RaW Suite Master Requirements ---
#
# EVERY entry is pinned. The versions below are the exact set installed in the
# production image (`pip freeze` inside the running singularity_core container,
# Python 3.11.15), which has been serving without incident. Pinning is not
# pedantry here: on 2026-08-02 an unpinned `cinemagoer` picked up upstream
# 2026.06.27, which deleted the HTTP access system, and every fresh install
# crashed on the first upload. Only direct dependencies are pinned; transitive
# ones are left to the resolver so that arm64/armhf wheel selection stays free.
#
# To move a pin: bump it here, rebuild, and smoke-test the affected path.
setuptools==82.0.1

# Núcleo y TUI
rich==15.0.0
python-dotenv==1.2.2
requests[socks]==2.34.2
PySocks==1.7.1

# Web Dashboard
fastapi==0.136.3
uvicorn==0.49.0
jinja2==3.1.6
beautifulsoup4==4.15.0

# RawLoadrr & Subida
aiofiles==25.1.0
aiohttp==3.14.1
anitopy==2.1.1
# Ships BOTH the `bencode` and `bencodepy` top-level packages, which is why
# upload.py can `import bencodepy` and clients.py `import bencode` from a single
# distribution. Never add the standalone `bencodepy` project alongside it.
bencode.py==4.0.0
# cinemagoer 2026.06.27 removed the HTTP access system entirely and switched the
# default accessSystem from 'http' to 's3'. Cinemagoer() then builds an SQLAlchemy
# engine from the bogus default URI 'sqlite://cinemagoer.db', which SQLAlchemy 2.x
# rejects, so every construction raises ArgumentError before any lookup happens.
cinemagoer==2023.5.1
deluge-client==1.10.2
ffmpeg-python==0.2.0
guessit==3.8.0
language-data<=1.2.0; python_version<='3.8'
langcodes<=3.4.0; python_version<='3.8'
langcodes==3.5.1; python_version>'3.8'
nest_asyncio==1.6.0
packaging==26.2
pyimgbox==1.0.7
pyotp==2.9.0
pyoxipng==9.1.1
pymediainfo==7.0.1
qbittorrent-api<=2023.9.53; python_version<'3.8'
qbittorrent-api==2026.6.0; python_version>='3.8'
tinytag==2.2.1
torf==4.3.1
tmdbsimple==2.9.6
unidecode==1.4.0

# Recordrr — automatización de captura por navegador
# (usa Google Chrome del sistema vía channel="chrome"; NO descargar Chromium)
playwright==1.60.0
opencv-python-headless==4.13.0.92
simpleobsws==1.4.3

# Procesamiento de Video (VapourSynth)
# Nota: En Docker instalaremos la base por apt-get,
# pero estas librerías de python son los puentes.
#
# MUST match the VapourSynth release forged from source in the Dockerfile
# (`git clone -b R73`). The pip package is only the binding; a newer binding
# against the R73 C library is a version mismatch. Bump both together or neither.
vapoursynth==73
awsmfunc==1.3.5

# Intruso — recuperación de galerías en torrents de OTROS usuarios
# Control por PIEZA (piece_priority / set_piece_deadline / read_piece) para bajar
# sólo unas ventanas del fichero en vez del torrent entero. La API de qBittorrent
# no llega: sólo tiene prioridad por FICHERO y modo secuencial.
# Hay rueda manylinux para x86_64 Y aarch64 (cp39-cp313), así que la RPi de
# Singularity_Lite no compila nada.
libtorrent==2.1.1
