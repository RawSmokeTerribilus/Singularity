"""Recordrr — automated browser-capture recorder (Singularity module).

Drives a real logged-in Google Chrome through a show, reads the HTML5 <video>
element to know exactly when each episode starts/ends, and records clean
per-episode files. Captures the decoded pixels the browser already paints — no
DRM keys, no CDM extraction. Same legal posture as a manual OBS browser capture.
"""

__version__ = "0.1.0"
